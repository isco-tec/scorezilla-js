# Build outputs
dist/
coverage/

# Vendored contract registry — generated by the monorepo contract sync and
# hashed by the #407 drift guard. Prettier must NOT reformat it (any byte change
# breaks the source-sha256 check).
test/contract/

# Deps
node_modules/
pnpm-lock.yaml

# Changesets bookkeeping (auto-generated)
.changeset/*.md
!.changeset/README.md
# `pre.json` is rewritten by `changesets/action` during the Version PR; its
# array formatting collides with prettier (changesets expands, prettier wants
# inline). Skip it — this is tool state, not source we author.
.changeset/pre.json
